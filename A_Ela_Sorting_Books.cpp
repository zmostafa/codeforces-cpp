#include <bits/stdc++.h>

using namespace std;

int main(){

    int testCases;
    cin >> testCases;
    while(testCases--){
        int n , k ;
        string str;
        cin >> n >> k;
        cin >> str;
        vector<char> chrs(26);
        for(auto& ch : str)
            chrs[ch - 'a']++;
        
        
    }

    return 0;
}